<h1>Update Inventory Information</h1>

<%= form_with url: "/warehouse_products/#{@warehouse_product.id}", method: :patch, local: true do |f| %>
  <%= f.label :quantity, "Quantity:" %>
  <%= f.number_field :quantity %><br><br>
  <%= f.submit %>
<% end %>
